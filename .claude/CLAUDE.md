All config files, sockets and other needed assets should be created in / read from the
project directory by default, and be configurable through command-line options so that
the command can read and create files form their Filesystem Hierarchy Standard locations.
This allows running the command as non-root from the project directory by default.

